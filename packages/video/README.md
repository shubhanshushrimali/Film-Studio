The video machine lives in `packages/flow/src/gpu_backend`.

Run that server when you want clips. The plan writer does not call it.
The door is `POST /generate/t2v`, `/generate/i2v`, and `/generate/flf2v`.
